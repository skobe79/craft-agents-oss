# Cloudflare Pages configuration for local development
# Production settings are configured in the Cloudflare Dashboard

name = "craft-session-viewer"
compatibility_date = "2024-01-01"

# For local dev with wrangler pages dev
[[r2_buckets]]
binding = "SESSIONS"
bucket_name = "craft-agent-sessions"
